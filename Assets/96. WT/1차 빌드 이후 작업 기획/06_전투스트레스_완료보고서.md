# [코드 작업 완료 보고서] 전투 스트레스(광기) 누적 시스템 구현 (임포터 보완 및 리팩토링 완료)

## 1. 개요 및 구현 목적
* **목표**: 전투 중 발생 이벤트(피격, 치명타, 사망, 회피, 긍정 리액션 등) 기반 캐릭터 스트레스 수치(`Insanity`) 변동 처리 구현 및 0~100 범위 경계값 제한 동작 보증
* **특이사항**: 신규 필드 하드코딩 지양. 기존 `TraitSO.Type (E_TraitType)` 구조를 활용하여 런타임 긍정 리액션 여부를 역참조하여 판정하도록 정합성 리팩토링 반영.
* **임포터 수정**: 스킬 시트(`skill.tsv`) 임포트 시 `SINGLE` / `AOE` 등 대소문자 혼용 문자열을 안정적으로 파싱하여 `TargetingType` 에셋에 반영하도록 보완. 탭 누락에 의한 파싱 에러 방지를 위해 `Optional()` 체이닝 및 개행 문자 필터링 반영.

## 2. 변경 파일 목록 및 상세 구현 사항

### [MODIFY] [SkillDataParser.cs](file:///C:/Users/kot77/Desktop/Unity/Bond/Assets/Editor/DataSheet/Parser/SkillDataParser.cs)
* `TargetingTypeConverter` 구현: 대소문자 무시 파싱 및 `\r`, `\n` 개행 문자 필터링 적용 (`SINGLE\r` 등의 오작동 차단)
* `SkillDataMap` 맵 변경: `TargetingType` 필드에 `.Optional()`을 지정하여 일부 탭 누락 행에서의 CsvHelper 파싱 실패 회피 보증
* `Populate` 내부에 `TargetingType` 매핑 누락 복구

### [MODIFY] [BaseCharacter.Anomaly.cs](file:///C:/Users/kot77/Desktop/Unity/Bond/Assets/02.%20Scripts/ReactionSystem/BaseCharacter.Anomaly.cs)
* `IsPositiveReaction(Reaction reaction)` 헬퍼 메서드 추가
* 런타임에 캐릭터 성향 리액션 슬롯(`TraitReactions`) 중 매칭 리액션을 추적하여, 해당 성향(`TraitSO`)의 분류가 `E_TraitType.Positive` 인지 판정 지원

### [MODIFY] [BattlePipeLineSo.cs](file:///C:/Users/kot77/Desktop/Unity/Bond/Assets/03.%20PipeLine/BattlePipeLineSo.cs)
* **공격 회피당함 (`STR_TRG_MISS`)**: `EvasionStep.Execute`에서 회피 판정 성공 시 시전자 스트레스 +5 증가 (`ReduceInsanity(5)`)
* **치명타 공격 성공 (`STR_TRG_CRIT_ATK`)**: `CriticalStep.Execute`에서 치명타 판정 성공 시 시전자 스트레스 -5 감소 (`RecoverInsanity(5)`)
* **긍정 리액션 발동 (`STR_TRG_POSITIVE_REACTION`)**: `ReactionCall.Execute`에서 `execution.Agent.IsPositiveReaction(execution.Reaction)` 감지 시 아군 전체 스트레스 -5 감소 (`RecoverInsanity(5)`)
* **피격 판정 (`STR_TRG_HIT` & `STR_TRG_CRIT_HIT`)**: `ApplyStep.Execute`에서 공격 스킬 적용 완료 시 피격자의 스트레스 일반 +3 / 치명타 +10 증가 (`ReduceInsanity`)

### [MODIFY] [BattleManager.cs](file:///C:/Users/kot77/Desktop/Unity/Bond/Assets/02.%20Scripts/BattleSystem/BattleManager.cs)
* **아군 사망 (`STR_TRG_ALLY_DEAD`)**: `HandleCharacterDeath` 시점에 사망 유닛 제외 생존 아군 전체 스트레스 +20 증가 (`ReduceInsanity(20)`)

---

## 3. 검증 결과 요약
* 임포터 파싱 보증: 시트의 `SINGLE` / `AOE` 데이터가 유실 및 예외 발생 없이 `SkillData` 에셋에 반영됨을 검증
* 기존 데이터 구조 활용: 임의의 `IsPositive` 필드 추가를 롤백하고, 기존 성향 Enum(`E_TraitType`)을 통해 긍정 리액션을 안전하고 유연하게 식별 확인
* 수치 제한 검증: `Insanity` 게이지 변동 처리 시 `BaseCharacter` 내부의 `Mathf.Clamp`에 의해 0 미만 / 100 초과가 정상 차단됨을 보증
